
const eq = require('./eq')
const ne = require('./ne')
const lt = require('./lt')
const le = require('./le')
const gt = require('./gt')
const ge = require('./ge')
const or = require('./or')
const and = require('./and')

module.exports = {
    eq,
    ne,
    lt,
    le,
    gt,
    ge,
    or,
    and,
}